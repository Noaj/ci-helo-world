version: 2.1

commands:
  destroy_environment:
    steps:
      - run:
          name: Destroy environment
          command: echo destroyed ec2
          when: on_fail

jobs:
  create_infrastructure:
    docker:
      - image: amazon/aws-cli
    steps:
      - checkout
      - run:
          name: Create Stack
          command: echo created ec2
      - run: return 1
      - destroy_environment

workflows:
  default:
    jobs:
      - build